# Planned AI application packages

Các package `api`, `clients`, `core`, `pipelines`, `repositories`, `schemas`, `services` và `workers` sẽ được tạo khi bắt đầu AI-M0. Hiện chưa có Python implementation.
